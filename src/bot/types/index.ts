export type TData = {
  note: string
  product: string
}

export type TProduct = {
  id: string
  name: string
  bar: string
  xmlBar: string
}

export type TNote = {
  description: string
  code: string
  number: string
}

export type TReport = {
  nf: string
  products: string[]
}
